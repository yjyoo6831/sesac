<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        span {
          color: blue;
          font-weight: 700;
        }
      </style>
</head>
<body>
    <h1><%= title %></h1>
    <div><span><%= userInfo.id %></span>님 환영합니다!</div>
    <div>비밀번호는 <span><%= userInfo.pw %></span>이군요!!</div>
    <div>이메일은 <span><%= userInfo.email %></span>이군요!!</div>
    <% if(userInfo.grade) { %>

      <div>학년은 <span> <%= userInfo.grade %></span>이군요!!</div>

      <% } else { %>
      <% alert('학년 미입력') } %>
    

    
</body>
</html>